package org.supsi;
import java.io.IOException;

/*
 * IMPORTANTE! per eseguire il codice da ide (intellij):
 * File > Settings > Build, Execution, Deployment > Compiler > Java Compiler.
 * DISABILITA IL FLAG IN: Use '--release' option for cross-compilation (Java 9 and later)
 *
 */
public class Main {
    public static void main(String[] args) throws IOException {
        MainFx.main(args);
    }
}